describe('PartnerPage', () => {
  it(
    'should display a message indicating the upcoming redirection if user has a simulation'
  )

  it('should redirect to /404 if no partner search param is provided')

  it("should redirect to /404 if the partner isn't verified")

  it(
    "should redirect to /simulateur/bilan if the user hasn't completed his/her test"
  )
})
