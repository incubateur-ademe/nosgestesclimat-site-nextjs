DATABASE_URL=postgresql://postgres:postgres@localhost:5432/ngc # no default
SEED_ADMIN_EMAILS=demo+admin1@example.com,demo+admin2@example.com # REQUIRED by the demo seed: comma-separated admin accounts; each gets its own demo organisation/polls/simulations. The seed fails if unset. Set the real list in the Scalingo dashboard (review apps only).