{
  "jobs": [
    {
      "command": "0 0 * * * pnpm jobs:computePollStats",
      "size": "M"
    },
    {
      "command": "0 7 * * * pnpm jobs:recoverMatomoStats",
      "size": "M"
    },

    {
      "command": "0 3 * * * pnpm jobs:syncNotionActions; pnpm jobs:cleanupExpiredTokens; pnpm jobs:syncBrevoContacts ; pnpm jobs:recoverNewsletterStats",
      "size": "M"
    },
    {
      "command": "*/15 * * * * pnpm jobs:refreshEventComputation",
      "size": "S"
    }
  ]
}
